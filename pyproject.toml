[project]
name = "todo-console-app"
version = "0.1.0"
description = "Phase I: In-memory CLI todo application"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
todo = "src.main:main"
