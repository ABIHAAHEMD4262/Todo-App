apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  namespace: todo-app
data:
  api-url: "http://localhost:8000"
  frontend-url: "http://localhost:3000"
  log-level: "info"
  environment: "production"
